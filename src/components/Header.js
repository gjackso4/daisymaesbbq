import React from 'react';

const Header = (props) => {
   return (
      <header className="top">
         <img src="/images/logo.jpg" alt="Daisy Mae's Logo" />
      </header>
   )
}

export default Header;